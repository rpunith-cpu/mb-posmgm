MediBuddy Position Management - Demo repo

Run frontend:
cd frontend
npm install
npm run dev

Run backend:
cd backend
npm install
node index.js

Frontend demo is minimal; to connect frontend to backend replace fetch calls to http://localhost:8080/api/positions
